{
  "name": "@healthhub/worker",
  "version": "0.1.0",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "cf-typegen": "wrangler types",
    "db:migrate:local": "wrangler d1 migrations apply healthhub --local",
    "db:migrate:remote": "wrangler d1 migrations apply healthhub --remote"
  },
  "dependencies": {
    "hono": "^4.12.32",
    "hyparquet-writer": "^0.16.1"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.18.8",
    "@cloudflare/workers-types": "^5.20260727.1",
    "vitest": "^4.1.10",
    "wrangler": "^4.114.0"
  }
}
